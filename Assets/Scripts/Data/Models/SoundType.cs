using UnityEngine;

namespace Data
{
    public enum SoundType
    {
        OnButtonClick,
        OnGameDraw,
        OnGameWin,
        OnGameStart,
        OnPieceDrop,
        OnButtonHover
    }
}